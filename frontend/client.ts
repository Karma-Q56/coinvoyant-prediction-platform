// Code generated by the Encore 1.51.6 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/
import type { CookieWithOptions } from "encore.dev/api";

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the  Encore application.
 */
export class Client {
    public readonly admin: admin.ServiceClient
    public readonly challenge: challenge.ServiceClient
    public readonly prediction: prediction.ServiceClient
    public readonly sweepstakes: sweepstakes.ServiceClient
    public readonly user: user.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.admin = new admin.ServiceClient(base)
        this.challenge = new challenge.ServiceClient(base)
        this.prediction = new prediction.ServiceClient(base)
        this.sweepstakes = new sweepstakes.ServiceClient(base)
        this.user = new user.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { checkAdmin as api_admin_check_admin_checkAdmin } from "~backend/admin/check_admin";
import { createPrediction as api_admin_create_prediction_createPrediction } from "~backend/admin/create_prediction";
import { createSamplePredictions as api_admin_create_sample_predictions_createSamplePredictions } from "~backend/admin/create_sample_predictions";
import { createSweepstakes as api_admin_create_sweepstakes_createSweepstakes } from "~backend/admin/create_sweepstakes";
import { getStats as api_admin_get_stats_getStats } from "~backend/admin/get_stats";
import { resolvePrediction as api_admin_resolve_prediction_resolvePrediction } from "~backend/admin/resolve_prediction";

export namespace admin {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.checkAdmin = this.checkAdmin.bind(this)
            this.createPrediction = this.createPrediction.bind(this)
            this.createSamplePredictions = this.createSamplePredictions.bind(this)
            this.createSweepstakes = this.createSweepstakes.bind(this)
            this.getStats = this.getStats.bind(this)
            this.resolvePrediction = this.resolvePrediction.bind(this)
        }

        /**
         * Checks if a user has admin privileges based on their email.
         */
        public async checkAdmin(params: { userId: number }): Promise<ResponseType<typeof api_admin_check_admin_checkAdmin>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/check/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_check_admin_checkAdmin>
        }

        /**
         * Creates a new prediction (admin only).
         */
        public async createPrediction(params: RequestType<typeof api_admin_create_prediction_createPrediction>): Promise<ResponseType<typeof api_admin_create_prediction_createPrediction>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/predictions`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_create_prediction_createPrediction>
        }

        /**
         * Creates sample predictions for testing (admin only).
         */
        public async createSamplePredictions(params: RequestType<typeof api_admin_create_sample_predictions_createSamplePredictions>): Promise<ResponseType<typeof api_admin_create_sample_predictions_createSamplePredictions>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/create-sample-predictions`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_create_sample_predictions_createSamplePredictions>
        }

        /**
         * Creates a new sweepstakes (admin only).
         */
        public async createSweepstakes(params: RequestType<typeof api_admin_create_sweepstakes_createSweepstakes>): Promise<ResponseType<typeof api_admin_create_sweepstakes_createSweepstakes>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/sweepstakes`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_create_sweepstakes_createSweepstakes>
        }

        /**
         * Gets admin dashboard statistics.
         */
        public async getStats(params: { userId: number }): Promise<ResponseType<typeof api_admin_get_stats_getStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/stats/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_get_stats_getStats>
        }

        /**
         * Resolves a prediction and distributes rewards (admin only).
         */
        public async resolvePrediction(params: RequestType<typeof api_admin_resolve_prediction_resolvePrediction>): Promise<ResponseType<typeof api_admin_resolve_prediction_resolvePrediction>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                correctOption: params.correctOption,
                userId:        params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/predictions/${encodeURIComponent(params.predictionId)}/resolve`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_resolve_prediction_resolvePrediction>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { acceptChallenge as api_challenge_accept_challenge_acceptChallenge } from "~backend/challenge/accept_challenge";
import { createChallenge as api_challenge_create_challenge_createChallenge } from "~backend/challenge/create_challenge";
import { getPendingChallenges as api_challenge_get_pending_challenges_getPendingChallenges } from "~backend/challenge/get_pending_challenges";
import { listChallenges as api_challenge_list_challenges_listChallenges } from "~backend/challenge/list_challenges";

export namespace challenge {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.acceptChallenge = this.acceptChallenge.bind(this)
            this.createChallenge = this.createChallenge.bind(this)
            this.getPendingChallenges = this.getPendingChallenges.bind(this)
            this.listChallenges = this.listChallenges.bind(this)
        }

        public async acceptChallenge(params: RequestType<typeof api_challenge_accept_challenge_acceptChallenge>): Promise<ResponseType<typeof api_challenge_accept_challenge_acceptChallenge>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/challenge/accept`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_challenge_accept_challenge_acceptChallenge>
        }

        public async createChallenge(params: RequestType<typeof api_challenge_create_challenge_createChallenge>): Promise<ResponseType<typeof api_challenge_create_challenge_createChallenge>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/challenge/create`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_challenge_create_challenge_createChallenge>
        }

        public async getPendingChallenges(): Promise<ResponseType<typeof api_challenge_get_pending_challenges_getPendingChallenges>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/challenge/pending`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_challenge_get_pending_challenges_getPendingChallenges>
        }

        public async listChallenges(): Promise<ResponseType<typeof api_challenge_list_challenges_listChallenges>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/challenge/list`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_challenge_list_challenges_listChallenges>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { getUserPredictions as api_prediction_get_user_predictions_getUserPredictions } from "~backend/prediction/get_user_predictions";
import { listPredictions as api_prediction_list_predictions_listPredictions } from "~backend/prediction/list_predictions";
import { vote as api_prediction_vote_vote } from "~backend/prediction/vote";

export namespace prediction {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getUserPredictions = this.getUserPredictions.bind(this)
            this.listPredictions = this.listPredictions.bind(this)
            this.vote = this.vote.bind(this)
        }

        /**
         * Gets all predictions a user has voted on.
         */
        public async getUserPredictions(params: { userId: number }): Promise<ResponseType<typeof api_prediction_get_user_predictions_getUserPredictions>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/predictions/user/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_prediction_get_user_predictions_getUserPredictions>
        }

        /**
         * Lists all predictions with optional filtering.
         */
        public async listPredictions(params: RequestType<typeof api_prediction_list_predictions_listPredictions>): Promise<ResponseType<typeof api_prediction_list_predictions_listPredictions>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category:       params.category,
                predictionType: params.predictionType,
                status:         params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/predictions`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_prediction_list_predictions_listPredictions>
        }

        /**
         * Submits a vote on a prediction.
         */
        public async vote(params: RequestType<typeof api_prediction_vote_vote>): Promise<ResponseType<typeof api_prediction_vote_vote>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/predictions/vote`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_prediction_vote_vote>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { enter as api_sweepstakes_enter_enter } from "~backend/sweepstakes/enter";
import { getUserEntries as api_sweepstakes_get_user_entries_getUserEntries } from "~backend/sweepstakes/get_user_entries";
import { listSweepstakes as api_sweepstakes_list_sweepstakes_listSweepstakes } from "~backend/sweepstakes/list_sweepstakes";

export namespace sweepstakes {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.enter = this.enter.bind(this)
            this.getUserEntries = this.getUserEntries.bind(this)
            this.listSweepstakes = this.listSweepstakes.bind(this)
        }

        /**
         * Enters a user into a sweepstakes.
         */
        public async enter(params: RequestType<typeof api_sweepstakes_enter_enter>): Promise<ResponseType<typeof api_sweepstakes_enter_enter>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sweepstakes/enter`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sweepstakes_enter_enter>
        }

        /**
         * Gets all sweepstakes entries for a user.
         */
        public async getUserEntries(params: { userId: number }): Promise<ResponseType<typeof api_sweepstakes_get_user_entries_getUserEntries>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sweepstakes/user/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sweepstakes_get_user_entries_getUserEntries>
        }

        /**
         * Lists all available sweepstakes.
         */
        public async listSweepstakes(): Promise<ResponseType<typeof api_sweepstakes_list_sweepstakes_listSweepstakes>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sweepstakes`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sweepstakes_list_sweepstakes_listSweepstakes>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { checkAchievements as api_user_check_achievements_checkAchievements } from "~backend/user/check_achievements";
import { checkDailyBonus as api_user_check_daily_bonus_checkDailyBonus } from "~backend/user/check_daily_bonus";
import { claimDailyBonus as api_user_claim_daily_bonus_claimDailyBonus } from "~backend/user/claim_daily_bonus";
import { generateChallengeId as api_user_generate_challenge_id_generateChallengeId } from "~backend/user/generate_challenge_id";
import { getAchievements as api_user_get_achievements_getAchievements } from "~backend/user/get_achievements";
import { getDashboard as api_user_get_dashboard_getDashboard } from "~backend/user/get_dashboard";
import { getEnhancedProfile as api_user_get_enhanced_profile_getEnhancedProfile } from "~backend/user/get_enhanced_profile";
import { getLeaderboard as api_user_get_leaderboard_getLeaderboard } from "~backend/user/get_leaderboard";
import { getLeaderboards as api_user_get_leaderboards_getLeaderboards } from "~backend/user/get_leaderboards";
import { getProfile as api_user_get_profile_getProfile } from "~backend/user/get_profile";
import { getTransactions as api_user_get_transactions_getTransactions } from "~backend/user/get_transactions";
import { listAchievements as api_user_list_achievements_listAchievements } from "~backend/user/list_achievements";
import { login as api_user_login_login } from "~backend/user/login";
import { monthlyReset as api_user_monthly_reset_monthlyReset } from "~backend/user/monthly_reset";
import { purchaseTokens as api_user_purchase_tokens_purchaseTokens } from "~backend/user/purchase_tokens";
import { register as api_user_register_register } from "~backend/user/register";
import { upgradePremium as api_user_upgrade_premium_upgradePremium } from "~backend/user/upgrade_premium";
import { watchAd as api_user_watch_ad_watchAd } from "~backend/user/watch_ad";

export namespace user {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.checkAchievements = this.checkAchievements.bind(this)
            this.checkDailyBonus = this.checkDailyBonus.bind(this)
            this.claimDailyBonus = this.claimDailyBonus.bind(this)
            this.generateChallengeId = this.generateChallengeId.bind(this)
            this.getAchievements = this.getAchievements.bind(this)
            this.getDashboard = this.getDashboard.bind(this)
            this.getEnhancedProfile = this.getEnhancedProfile.bind(this)
            this.getLeaderboard = this.getLeaderboard.bind(this)
            this.getLeaderboards = this.getLeaderboards.bind(this)
            this.getProfile = this.getProfile.bind(this)
            this.getTransactions = this.getTransactions.bind(this)
            this.listAchievements = this.listAchievements.bind(this)
            this.login = this.login.bind(this)
            this.monthlyReset = this.monthlyReset.bind(this)
            this.purchaseTokens = this.purchaseTokens.bind(this)
            this.register = this.register.bind(this)
            this.upgradePremium = this.upgradePremium.bind(this)
            this.watchAd = this.watchAd.bind(this)
        }

        public async checkAchievements(): Promise<ResponseType<typeof api_user_check_achievements_checkAchievements>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/check-achievements`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_check_achievements_checkAchievements>
        }

        /**
         * Checks if user can claim daily login bonus.
         */
        public async checkDailyBonus(params: { userId: number }): Promise<ResponseType<typeof api_user_check_daily_bonus_checkDailyBonus>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/daily-bonus`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_check_daily_bonus_checkDailyBonus>
        }

        /**
         * Claims the daily login bonus.
         */
        public async claimDailyBonus(params: { userId: number }): Promise<ResponseType<typeof api_user_claim_daily_bonus_claimDailyBonus>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/claim-daily-bonus`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_claim_daily_bonus_claimDailyBonus>
        }

        public async generateChallengeId(): Promise<ResponseType<typeof api_user_generate_challenge_id_generateChallengeId>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/generate-challenge-id`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_generate_challenge_id_generateChallengeId>
        }

        /**
         * Gets all achievements for a user with their progress.
         */
        public async getAchievements(params: { userId: number }): Promise<ResponseType<typeof api_user_get_achievements_getAchievements>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/achievements`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_achievements_getAchievements>
        }

        /**
         * Gets dashboard data for a user.
         */
        public async getDashboard(params: { userId: number }): Promise<ResponseType<typeof api_user_get_dashboard_getDashboard>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/dashboard`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_dashboard_getDashboard>
        }

        public async getEnhancedProfile(params: { userId: number }): Promise<ResponseType<typeof api_user_get_enhanced_profile_getEnhancedProfile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/enhanced-profile`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_enhanced_profile_getEnhancedProfile>
        }

        /**
         * Gets the leaderboard data.
         */
        public async getLeaderboard(): Promise<ResponseType<typeof api_user_get_leaderboard_getLeaderboard>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/leaderboard`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_leaderboard_getLeaderboard>
        }

        public async getLeaderboards(): Promise<ResponseType<typeof api_user_get_leaderboards_getLeaderboards>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/leaderboards`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_leaderboards_getLeaderboards>
        }

        /**
         * Gets a user's profile information.
         */
        public async getProfile(params: { userId: number }): Promise<ResponseType<typeof api_user_get_profile_getProfile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/profile`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_profile_getProfile>
        }

        /**
         * Gets the last 10 transactions for a user.
         */
        public async getTransactions(params: { userId: number }): Promise<ResponseType<typeof api_user_get_transactions_getTransactions>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/${encodeURIComponent(params.userId)}/transactions`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_transactions_getTransactions>
        }

        public async listAchievements(): Promise<ResponseType<typeof api_user_list_achievements_listAchievements>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/achievements`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_list_achievements_listAchievements>
        }

        /**
         * Logs in a user with email and password.
         */
        public async login(params: RequestType<typeof api_user_login_login>): Promise<ResponseType<typeof api_user_login_login>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_login_login>
        }

        public async monthlyReset(): Promise<ResponseType<typeof api_user_monthly_reset_monthlyReset>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/monthly-reset`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_monthly_reset_monthlyReset>
        }

        /**
         * Processes a token purchase (ET + bonus PT).
         */
        public async purchaseTokens(params: RequestType<typeof api_user_purchase_tokens_purchaseTokens>): Promise<ResponseType<typeof api_user_purchase_tokens_purchaseTokens>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/purchase-tokens`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_purchase_tokens_purchaseTokens>
        }

        /**
         * Registers a new user account.
         */
        public async register(params: RequestType<typeof api_user_register_register>): Promise<ResponseType<typeof api_user_register_register>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/register`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_register_register>
        }

        public async upgradePremium(): Promise<ResponseType<typeof api_user_upgrade_premium_upgradePremium>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/upgrade-premium`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_upgrade_premium_upgradePremium>
        }

        public async watchAd(): Promise<ResponseType<typeof api_user_watch_ad_watchAd>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/user/watch-ad`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_watch_ad_watchAd>
        }
    }
}


type PickMethods<Type> = Omit<CallParameters, "method"> & { method?: Type };

// Helper type to omit all fields that are cookies.
type OmitCookie<T> = {
  [K in keyof T as T[K] extends CookieWithOptions<any> ? never : K]: T[K];
};

type RequestType<Type extends (...args: any[]) => any> =
  Parameters<Type> extends [infer H, ...any[]]
    ? OmitCookie<H>
    : void;

type ResponseType<Type extends (...args: any[]) => any> = OmitCookie<Awaited<ReturnType<Type>>>;

function dateReviver(key: string, value: any): any {
  if (
    typeof value === "string" &&
    value.length >= 10 &&
    value.charCodeAt(0) >= 48 && // '0'
    value.charCodeAt(0) <= 57 // '9'
  ) {
    const parsedDate = new Date(value);
    if (!isNaN(parsedDate.getTime())) {
      return parsedDate;
    }
  }
  return value;
}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

import {
  StreamInOutHandlerFn,
  StreamInHandlerFn,
  StreamOutHandlerFn,
} from "encore.dev/api";

type StreamRequest<Type> = Type extends
  | StreamInOutHandlerFn<any, infer Req, any>
  | StreamInHandlerFn<any, infer Req, any>
  | StreamOutHandlerFn<any, any>
  ? Req
  : never;

type StreamResponse<Type> = Type extends
  | StreamInOutHandlerFn<any, any, infer Resp>
  | StreamInHandlerFn<any, any, infer Resp>
  | StreamOutHandlerFn<any, infer Resp>
  ? Resp
  : never;


function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data, dateReviver))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}


// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "-Generated-TS-Client (Encore/1.51.6)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(path: string, params?: CallParameters): Promise<Response> {
        return this.callAPI(path, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(path: string, params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}

export default new Client(import.meta.env.VITE_CLIENT_TARGET, { requestInit: { credentials: "include" } });
